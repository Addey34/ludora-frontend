/node_modules
/dist

# Claude Code: local/personal settings (shared settings go in settings.json)
.claude/settings.local.json
CLAUDE.md

# Local environment and private notes (never commit - personal machine only)
.env
.env.local
.env.*.local

# Private docs
docs/private/

# Firebase deploy cache
.firebase/
